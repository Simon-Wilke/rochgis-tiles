<<<<<<< HEAD
name: Generate & Deploy Tiles

on:
  # Manual trigger
  workflow_dispatch:
  # Weekly refresh — every Sunday at 03:00 UTC
  schedule:
    - cron: "0 3 * * 0"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false
=======
name: RochGIS Tile Generator

on:
  workflow_dispatch:
>>>>>>> 75cd6fbe1782bb6eb3536c8ba628ab57c7a45384

jobs:
  build:
    runs-on: ubuntu-latest
<<<<<<< HEAD
    timeout-minutes: 60

    steps:
      - name: Checkout repository
=======

    steps:
      - name: Checkout repo
>>>>>>> 75cd6fbe1782bb6eb3536c8ba628ab57c7a45384
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

<<<<<<< HEAD
      - name: Install Tippecanoe
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential libsqlite3-dev zlib1g-dev
          git clone https://github.com/felt/tippecanoe.git /tmp/tippecanoe
          cd /tmp/tippecanoe
          make -j$(nproc)
          sudo make install
          tippecanoe --version

      - name: Download parcel data
        run: python scripts/download_parcels.py

      - name: Generate vector tiles
        run: bash scripts/generate_tiles.sh

      - name: Validate output
        run: python scripts/validate_tiles.py

      - name: Prepare Pages artifact
        run: |
          mkdir -p _site
          cp -r tiles _site/tiles
          # Include a metadata file for debugging
          echo '{"generated":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'","repo":"rochgis-tiles"}' > _site/metadata.json

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
=======
      - name: Install dependencies
        run: |
          pip install requests

      - name: Download parcels
        run: |
          python scripts/download_parcels.py

      - name: Install Tippecanoe
        run: |
          sudo apt-get update
          sudo apt-get install -y tippecanoe

      - name: Generate tiles
        run: |
          bash scripts/generate_tiles.sh

      - name: Validate output
        run: |
          python scripts/validate_tiles.py

      - name: Upload tiles artifact
        uses: actions/upload-artifact@v4
        with:
          name: tiles
          path: tiles
>>>>>>> 75cd6fbe1782bb6eb3536c8ba628ab57c7a45384
